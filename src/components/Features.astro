---
const features = [
  {
    icon: "speed",
    title: "Rendimiento y Seguridad Optimizados",
    description: "Desarrollo web de alto rendimiento con tiempos de carga ultrarrápidos y protección avanzada contra amenazas."
  },
  {
    icon: "design",
    title: "Diseño Personalizado y Único",
    description: "Diseños exclusivos y modernos que reflejan la identidad de tu marca y conectan con tu audiencia."
  },
  {
    icon: "seo",
    title: "SEO y Visibilidad Online",
    description: "Optimización profesional para motores de búsqueda que mejora tu ranking en Google y aumenta el tráfico orgánico."
  },
  {
    icon: "support",
    title: "Mantenimiento y Soporte Técnico",
    description: "Actualizaciones regulares, copias de seguridad y soporte técnico continuo para mantener tu web siempre al día."
  }
];
---

<section class="py-20" id="servicios">
  <div class="container">
    <div class="text-center mb-16" data-sr>
      <h2 class="text-3xl font-bold mb-4">¿Por qué trabajar conmigo?</h2>
      <p class="text-text-muted max-w-2xl mx-auto">Desarrollo sitios web profesionales que no solo se ven increíbles, sino que también generan resultados medibles para tu negocio.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {features.map((feature, index) => (
        <div class="card feature-card" data-sr data-sr-delay={index * 100}>
          <div class="feature-icon mb-4">
            {feature.icon === "speed" && (
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            )}
            {feature.icon === "design" && (
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
              </svg>
            )}
            {feature.icon === "seo" && (
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
            )}
            {feature.icon === "support" && (
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192l-3.536 3.536M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-5 0a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
            )}
          </div>
          <h3 class="text-xl font-bold mb-3">{feature.title}</h3>
          <p class="text-text-muted">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .feature-card {
    transition: transform 0.3s ease;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .feature-card:hover {
    transform: translateY(-4px);
  }

  .feature-icon {
    color: var(--color-accent);
  }

  .text-text-muted {
    color: var(--color-text-muted);
  }
</style> 