---
const pricingTiers = [
  {
    name: "Landing Page",
    price: "350",
    features: [
      "1 página completa",
      "Diseño personalizado",
      "Formulario de contacto",
      "Optimizado para móviles",
      "Dominio y hosting 1 año"
    ]
  },
  {
    name: "Web Completa",
    price: "600",
    features: [
      "Hasta 5 páginas",
      "Diseño personalizado",
      "Blog o noticias",
      "Panel de administración",
      "SEO básico incluido",
      "Dominio y hosting 1 año"
    ],
    popular: true
  },
  {
    name: "Tienda Online",
    price: "800",
    features: [
      "Tienda completa",
      "Sistema de pagos",
      "Gestión de productos",
      "Panel de administración",
      "SEO avanzado",
      "Dominio y hosting 1 año"
    ]
  }
];
---

<section class="py-20 bg-secondary" id="precios">
  <div class="container">
    <div class="text-center mb-16">
      <h2 class="text-3xl font-bold mb-4">Planes y Precios</h2>
      <p class="text-text-muted max-w-2xl mx-auto">Soluciones profesionales adaptadas a diferentes necesidades y presupuestos.</p>
      <p class="text-text-muted mt-2">El precio final puede ajustarse según los requisitos específicos de tu proyecto.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {pricingTiers.map(tier => (
        <div class={`card pricing-card ${tier.popular ? 'popular' : ''}`}>
          {tier.popular && (
            <div class="popular-badge">Más Popular</div>
          )}
          <h3 class="text-2xl font-bold mb-2">{tier.name}</h3>
          <div class="price-container mb-6">
            <span class="text-sm">Desde</span>
            <div class="flex items-baseline gap-1">
              <span class="text-4xl font-bold">{tier.price}</span>
              <span class="text-xl">€</span>
            </div>
          </div>
          <ul class="space-y-3 mb-8 flex-grow">
            {tier.features.map(feature => (
              <li class="flex items-center gap-2">
                <svg class="h-5 w-5 text-accent flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                <span>{feature}</span>
              </li>
            ))}
          </ul>
          <div class="mt-auto">
            <a href="#contacto" class={`btn-${tier.popular ? 'primary' : 'secondary'} w-full text-center`}>
              Solicitar presupuesto
            </a>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .pricing-card {
    display: flex;
    flex-direction: column;
    height: 100%;
    position: relative;
    transition: transform 0.3s ease;
    background-color: var(--color-card-bg);
  }

  .pricing-card:hover {
    transform: translateY(-4px);
  }

  .pricing-card.popular {
    border-color: var(--color-accent);
    background-color: var(--color-card-bg);
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }

  .popular-badge {
    position: absolute;
    top: 0;
    right: 1.5rem;
    transform: translateY(-50%);
    background-color: var(--color-accent);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 1rem;
    font-size: 0.875rem;
    font-weight: 500;
  }

  .price-container {
    color: var(--color-text);
  }

  .text-accent {
    color: var(--color-accent);
  }

  .text-text-muted {
    color: var(--color-text-muted);
  }
</style> 