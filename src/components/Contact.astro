---
import { getLocaleFromPath } from '../i18n/utils';
import { siteContent } from '../data/siteContent';

const locale = getLocaleFromPath(Astro.url.pathname);
const content = siteContent[locale].contact;

const projectTypes =
  locale === 'en'
    ? [
        ['landing', 'Landing page'],
        ['web', 'Business website'],
        ['redesign', 'Redesign / improvement'],
        ['laravel', 'Laravel project'],
        ['webapp', 'Web app / SaaS / MVP'],
        ['maintenance', 'Maintenance'],
        ['bugfix', 'Fix / technical issue'],
        ['other', 'Other'],
      ]
    : [
        ['landing', 'Landing page'],
        ['web', 'Web de empresa'],
        ['redesign', 'Rediseno / mejora'],
        ['laravel', 'Proyecto Laravel'],
        ['webapp', 'App web / SaaS / MVP'],
        ['maintenance', 'Mantenimiento'],
        ['bugfix', 'Fix / problema tecnico'],
        ['other', 'Otro'],
      ];

const budgetOptions =
  locale === 'en'
    ? [
        ['', 'Prefer not to say'],
        ['under-1500', 'Under EUR 1,500'],
        ['1500-3000', 'EUR 1,500 to 3,000'],
        ['3000-6000', 'EUR 3,000 to 6,000'],
        ['6000-10000', 'EUR 6,000 to 10,000'],
        ['over-10000', 'Over EUR 10,000'],
      ]
    : [
        ['', 'Prefiero no indicarlo'],
        ['under-1500', 'Menos de 1.500 EUR'],
        ['1500-3000', '1.500 a 3.000 EUR'],
        ['3000-6000', '3.000 a 6.000 EUR'],
        ['6000-10000', '6.000 a 10.000 EUR'],
        ['over-10000', 'Mas de 10.000 EUR'],
      ];
---

<section class="section section-alt" id="contacto" data-contact-root>
  <div class="container">
    <div class="section-heading">
      <p class="section-eyebrow">{content.eyebrow}</p>
      <h2>{content.title}</h2>
      <p class="section-copy">{content.description}</p>
    </div>

    <div class="contact-grid">
      <aside class="contact-panel surface-card" data-sr>
        <div class="contact-panel__stack">
          <article class="contact-channel">
            <span>{locale === 'en' ? 'Recommended path' : 'Paso recomendado'}</span>
            <a href="#contact-form">{locale === 'en' ? 'Use the contact form' : 'Usar el formulario'}</a>
            <p>{locale === 'en' ? 'It is the clearest way to explain the project and get a useful first reply.' : 'Es la forma mas clara de explicar el proyecto y recibir una primera respuesta util.'}</p>
          </article>

          <article class="contact-channel">
            <span>{locale === 'en' ? 'What happens next' : 'Que pasa despues'}</span>
            <strong class="contact-channel__next">{locale === 'en' ? 'You will hear back within 24 business hours' : 'Recibiras respuesta en 24 horas laborables'}</strong>
            <p>{locale === 'en' ? 'A first reply with an initial direction, clarifying questions if needed, or a proposal outline.' : 'Una primera respuesta con orientacion inicial, preguntas si hacen falta, o un esquema de propuesta.'}</p>
          </article>
        </div>

        <div class="contact-panel__note">
          <span>{content.responseLabel}</span>
          <p>{content.responseText}</p>
        </div>
      </aside>

      <div class="contact-form-wrap surface-card" data-sr>
        <h3>{content.formTitle}</h3>

        <form id="contact-form" class="contact-form" method="POST" novalidate>
          <div class="hp-field" aria-hidden="true">
            <label for="website">Website</label>
            <input type="text" id="website" name="website" tabindex="-1" autocomplete="off" />
          </div>

          <div class="contact-form__row">
            <label for="name">{locale === 'en' ? 'Name' : 'Nombre'}</label>
            <input id="name" name="name" type="text" required autocomplete="name" />
          </div>

          <div class="contact-form__row">
            <label for="email">Email</label>
            <input id="email" name="email" type="email" required autocomplete="email" inputmode="email" />
          </div>

          <div class="contact-form__row">
            <label for="type">{locale === 'en' ? 'Project type' : 'Tipo de proyecto'}</label>
            <select id="type" name="type" required>
              <option value="">{locale === 'en' ? 'Select an option' : 'Selecciona una opcion'}</option>
              {projectTypes.map(([value, label]) => (
                <option value={value}>{label}</option>
              ))}
            </select>
          </div>

          <div class="contact-form__row">
            <label for="budget">
              {locale === 'en' ? 'Approximate budget' : 'Presupuesto aproximado'}
              <span class="contact-form__hint">{locale === 'en' ? '(optional — helps me tailor the proposal)' : '(opcional — ayuda a ajustar la propuesta)'}</span>
            </label>
            <select id="budget" name="budget">
              {budgetOptions.map(([value, label]) => (
                <option value={value}>{label}</option>
              ))}
            </select>
          </div>

          <div class="contact-form__row">
            <label for="message">{locale === 'en' ? 'Project context' : 'Contexto del proyecto'}</label>
            <textarea id="message" name="message" rows="5" required></textarea>
          </div>

          <details class="contact-form__optional">
            <summary>{content.optionalFields}</summary>

            <div class="contact-form__row">
              <label for="company">{locale === 'en' ? 'Company' : 'Empresa'}</label>
              <input id="company" name="company" type="text" autocomplete="organization" />
            </div>
          </details>

          <button type="submit" class="btn-primary">{locale === 'en' ? 'Send project details' : 'Enviar detalles del proyecto'}</button>
          <p class="contact-form__assurance">{locale === 'en' ? 'No commitment required. You will get a reply within 24 business hours.' : 'Sin compromiso. Recibiras respuesta en 24 horas laborables.'}</p>
          <p id="contact-feedback" class="contact-feedback" role="status" aria-live="polite" tabindex="-1" hidden></p>
        </form>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-grid {
    display: grid;
    grid-template-columns: minmax(280px, 0.82fr) minmax(0, 1.18fr);
    gap: 1rem;
  }

  .contact-panel,
  .contact-form-wrap {
    padding: 1.55rem;
  }

  .contact-panel__stack {
    display: grid;
    gap: 0.9rem;
  }

  .contact-channel,
  .contact-panel__note {
    padding: 1rem;
    border: 1px solid rgba(74, 54, 41, 0.1);
    border-radius: 1rem;
    background: rgba(255, 250, 244, 0.88);
  }

  .contact-channel span,
  .contact-panel__note span {
    display: block;
    margin-bottom: 0.45rem;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--theme-highlight);
  }

  .contact-channel a {
    display: inline-flex;
    margin-bottom: 0.4rem;
    font-weight: 700;
    max-width: 100%;
    overflow-wrap: anywhere;
    word-break: break-word;
  }

  .contact-channel p,
  .contact-panel__note p {
    color: var(--color-text-muted);
    line-height: 1.75;
  }

  .contact-panel__note {
    margin-top: 1rem;
  }

  .contact-form-wrap h3 {
    margin-bottom: 1rem;
  }

  .contact-form {
    display: grid;
    gap: 1rem;
  }

  .contact-form__row {
    display: grid;
    gap: 0.45rem;
  }

  .contact-form__row label,
  .contact-form__optional summary {
    font-weight: 700;
  }

  .contact-form__hint {
    display: block;
    margin-top: 0.18rem;
    font-size: 0.82rem;
    font-weight: 400;
    color: var(--color-text-muted);
  }

  .contact-form__assurance {
    margin-top: -0.25rem;
    font-size: 0.88rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  .contact-channel__next {
    display: block;
    margin-bottom: 0.4rem;
    font-size: 1rem;
    color: var(--theme-highlight);
  }

  .contact-form__row input,
  .contact-form__row textarea,
  .contact-form__row select {
    width: 100%;
    padding: 0.95rem 1rem;
    border: 1px solid var(--theme-field-border);
    border-radius: 1rem;
    background: var(--theme-field-bg);
    color: var(--color-text);
    transition: border-color 0.18s ease, background-color 0.18s ease, box-shadow 0.18s ease;
  }

  .contact-form__row input:hover,
  .contact-form__row textarea:hover,
  .contact-form__row select:hover {
    border-color: rgba(74, 54, 41, 0.24);
  }

  .contact-form__row input:focus,
  .contact-form__row textarea:focus,
  .contact-form__row select:focus {
    background: var(--theme-field-bg-focus);
    border-color: rgba(184, 104, 54, 0.38);
    box-shadow: 0 0 0 4px rgba(184, 104, 54, 0.08);
  }

  .contact-form__optional {
    display: grid;
    gap: 0.95rem;
    padding: 0.95rem 1rem;
    border: 1px solid rgba(74, 54, 41, 0.1);
    border-radius: 1rem;
    background: rgba(255, 250, 244, 0.7);
  }

  .contact-form__optional summary {
    cursor: pointer;
    color: var(--color-text);
  }

  .hp-field,
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
  }

  .contact-feedback {
    display: none;
    align-items: flex-start;
    gap: 0.7rem;
    padding: 0.95rem 1rem;
    border-radius: 1rem;
    border: 1px solid rgba(74, 54, 41, 0.12);
    background: rgba(255, 250, 244, 0.92);
    color: var(--color-text);
    line-height: 1.65;
  }

  .contact-feedback::before {
    content: '';
    flex: 0 0 0.8rem;
    width: 0.8rem;
    height: 0.8rem;
    margin-top: 0.35rem;
    border-radius: 999px;
    background: rgba(74, 54, 41, 0.22);
  }

  .contact-feedback[data-state='success'] {
    border-color: rgba(33, 68, 63, 0.18);
    background: rgba(241, 248, 246, 0.95);
    color: #21443f;
  }

  .contact-feedback[data-state='success']::before {
    background: #21443f;
  }

  .contact-feedback[data-state='error'] {
    border-color: rgba(160, 76, 56, 0.18);
    background: rgba(252, 244, 241, 0.95);
    color: #7d3d2f;
  }

  .contact-feedback[data-state='error']::before {
    background: #a04c38;
  }

  .contact-feedback[data-state='loading'] {
    border-color: rgba(184, 104, 54, 0.16);
    background: rgba(255, 247, 240, 0.96);
    color: var(--color-text);
  }

  .contact-feedback[data-state='loading']::before {
    background: var(--theme-highlight);
    animation: pulse-dot 1.2s ease-in-out infinite;
  }

  .contact-feedback.is-visible {
    display: flex;
  }

  @keyframes pulse-dot {
    0%,
    100% {
      opacity: 0.38;
      transform: scale(0.92);
    }
    50% {
      opacity: 1;
      transform: scale(1);
    }
  }

  @media (max-width: 1040px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 720px) {
    .contact-panel,
    .contact-form-wrap {
      padding: 1.28rem;
    }

    .contact-form__optional {
      padding: 1rem;
    }

    .contact-grid {
      gap: 1.15rem;
    }
  }
</style>

<script is:inline define:vars={{ locale }}>
  const messages = {
    es: {
      send: 'Enviar detalles del proyecto',
      sending: 'Enviando...',
      sendingDetail: 'Enviando tu mensaje...',
      required: 'Completa nombre, email, tipo de proyecto y mensaje.',
      success: 'Mensaje enviado correctamente.',
      error: 'No se pudo enviar el mensaje. Intentalo de nuevo mas tarde.',
    },
    en: {
      send: 'Send project details',
      sending: 'Sending...',
      sendingDetail: 'Sending your message...',
      required: 'Please complete name, email, project type and message.',
      success: 'Message sent successfully.',
      error: 'The message could not be sent. Please try again later.',
    },
  };

  function bindContactForm() {
    const form = document.getElementById('contact-form');
    if (!form || form.dataset.bound === 'true') return;

    form.dataset.bound = 'true';
    const feedback = document.getElementById('contact-feedback');
    const button = form.querySelector('button[type="submit"]');
    const t = messages[locale];

    function setFeedback(state, message) {
      if (!feedback) return;
      feedback.hidden = false;
      feedback.textContent = message;
      feedback.dataset.state = state;
      feedback.classList.add('is-visible');

      if (state !== 'loading') {
        feedback.focus({ preventScroll: true });
      }
    }

    function clearFeedback() {
      if (!feedback) return;
      feedback.hidden = true;
      feedback.textContent = '';
      feedback.dataset.state = '';
      feedback.classList.remove('is-visible');
    }

    form.addEventListener('submit', async (event) => {
      event.preventDefault();
      if (!button) return;

      const formData = new FormData(form);
      const payload = Object.fromEntries(formData.entries());
      payload.locale = locale;

      if (!payload.name || !payload.email || !payload.type || !payload.message) {
        setFeedback('error', t.required);
        return;
      }

      clearFeedback();
      button.disabled = true;
      button.setAttribute('aria-busy', 'true');
      button.textContent = t.sending;
      setFeedback('loading', t.sendingDetail);

      try {
        const response = await fetch('/api/contact', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            Accept: 'application/json',
          },
          body: JSON.stringify(payload),
        });

        const result = await response.json();

        if (!response.ok) {
          throw new Error(result.error || t.error);
        }

        form.reset();
        setFeedback('success', result.message || t.success);
      } catch (error) {
        setFeedback('error', error instanceof Error ? error.message : t.error);
      } finally {
        button.disabled = false;
        button.removeAttribute('aria-busy');
        button.textContent = t.send;
      }
    });
  }

  bindContactForm();
  document.addEventListener('astro:after-swap', bindContactForm);
</script>
