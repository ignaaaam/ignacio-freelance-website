---
import Layout from '../layouts/Layout.astro';
import Header from './Header.astro';
import Contact from './Contact.astro';
import StickyCTA from './StickyGTA.astro';

type CTA = {
  label: string;
  href: string;
  variant?: 'primary' | 'secondary';
};

type Fact = {
  label: string;
  value: string;
};

interface Props {
  title: string;
  description: string;
  lang: 'es' | 'en';
  canonicalUrl: string;
  keywords?: string[];
  additionalSchemas?: any[];
  alternateUrls?: { es: string; en: string };
  badge: string;
  pageTitle: string;
  lead: string;
  primaryCta: CTA;
  secondaryCta?: CTA;
  facts?: Fact[];
}

const {
  title,
  description,
  lang,
  canonicalUrl,
  keywords = [],
  additionalSchemas = [],
  alternateUrls,
  badge,
  pageTitle,
  lead,
  primaryCta,
  secondaryCta,
  facts = [],
} = Astro.props;
---

<Layout
  title={title}
  description={description}
  lang={lang}
  canonicalUrl={canonicalUrl}
  keywords={keywords}
  additionalSchemas={additionalSchemas}
  alternateUrls={alternateUrls}
>
  <Header />
  <main>
    <section class="section">
      <div class="container">
        <div class="content-page-hero surface-card" data-sr>
          <div class="content-page-hero__main">
            <span class="content-page-hero__badge">{badge}</span>
            <h1>{pageTitle}</h1>
            <p class="content-page-hero__lead">{lead}</p>
            <div class="content-page-hero__actions">
              <a href={primaryCta.href} class="btn-primary">{primaryCta.label}</a>
              {secondaryCta && <a href={secondaryCta.href} class="btn-secondary">{secondaryCta.label}</a>}
            </div>
          </div>

          {facts.length > 0 && (
            <div class="content-page-hero__facts">
              {facts.map((fact) => (
                <article class="content-page-hero__fact">
                  <span>{fact.label}</span>
                  <p>{fact.value}</p>
                </article>
              ))}
            </div>
          )}
        </div>
      </div>
    </section>

    <section class="content-page-body">
      <div class="container">
        <article class="content-page-body__card surface-card">
          <div class="content-page-body__inner">
            <slot />
          </div>
        </article>
      </div>
    </section>

    <Contact />
  </main>
  <StickyCTA />
</Layout>

<style>
  .content-page-hero {
    display: grid;
    grid-template-columns: minmax(0, 1.15fr) minmax(300px, 0.85fr);
    gap: 1.2rem;
    padding: 1.85rem;
    background:
      radial-gradient(circle at top left, rgba(31, 107, 255, 0.18), transparent 28%),
      linear-gradient(180deg, rgba(10, 12, 18, 0.98), rgba(13, 16, 24, 0.98));
    color: var(--theme-dark-text);
    border-color: rgba(255, 255, 255, 0.08);
  }

  .content-page-hero__badge {
    display: inline-flex;
    margin-bottom: 1rem;
    padding: 0.45rem 0.8rem;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.06);
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(246, 248, 252, 0.82);
  }

  .content-page-hero__lead {
    max-width: 62ch;
    margin-top: 1rem;
    color: var(--theme-dark-muted);
    font-size: 1.08rem;
    line-height: 1.78;
  }

  .content-page-hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem;
    margin-top: 1.35rem;
  }

  .content-page-hero__facts {
    display: grid;
    gap: 0.85rem;
  }

  .content-page-hero__fact {
    padding: 1rem 1.05rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1.15rem;
    background: rgba(255, 255, 255, 0.06);
  }

  .content-page-hero__fact span {
    display: block;
    margin-bottom: 0.45rem;
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(246, 248, 252, 0.68);
  }

  .content-page-hero__fact p {
    color: var(--theme-dark-text);
    line-height: 1.68;
  }

  .content-page-body {
    padding-bottom: 4rem;
  }

  .content-page-body__card {
    padding: 2rem;
  }

  .content-page-body__inner {
    max-width: 62rem;
    margin: 0 auto;
  }

  .content-page-body__inner :global(h2),
  .content-page-body__inner :global(h3),
  .content-page-body__inner :global(h4) {
    color: var(--color-text);
  }

  .content-page-body__inner :global(p),
  .content-page-body__inner :global(li),
  .content-page-body__inner :global(td),
  .content-page-body__inner :global(th) {
    color: var(--color-text);
    line-height: 1.8;
  }

  .content-page-body__inner :global(a) {
    color: var(--theme-highlight);
    text-decoration: underline;
    text-underline-offset: 0.18em;
  }

  .content-page-body__inner :global(.bg-secondary),
  .content-page-body__inner :global(.card) {
    border: 1px solid rgba(17, 19, 26, 0.08);
    border-radius: 1.2rem;
    background: rgba(247, 249, 253, 0.9);
  }

  .content-page-body__inner :global(table) {
    overflow: hidden;
    border: 1px solid rgba(74, 54, 41, 0.12);
    border-radius: 1rem;
  }

  .content-page-body__inner :global(.price-table thead tr) {
    background: rgba(184, 104, 54, 0.08);
  }

  @media (max-width: 1040px) {
    .content-page-hero {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 720px) {
    .content-page-hero,
    .content-page-body__card {
      padding: 1.35rem;
    }

    .content-page-hero__actions {
      flex-direction: column;
      gap: 0.72rem;
    }

    .content-page-hero__lead {
      font-size: 1rem;
      line-height: 1.76;
    }
  }
</style>
