---
import BlogIndexPage from '../../components/BlogIndexPage.astro';
import { getPublishedBlogEntries } from '../../data/blog';

const posts = await getPublishedBlogEntries('es');
---

<BlogIndexPage locale="es" posts={posts} />
